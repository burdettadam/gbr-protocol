[workspace]
members = [
    "reference/rust",      # cap-narrative-types: narrative profile types (enums, entities, schemas)
    "grimoire-tooling",    # authoring tools (gates, training, DAG, PyO3, bins)
]
resolver = "2"

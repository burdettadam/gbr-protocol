[workspace]
members = [
    "reference/rust",      # gbr-types: protocol-core (enums, entities, schemas)
    "grimoire-tooling",    # authoring tools (gates, training, DAG, PyO3, bins)
    "grimoire-types",      # backward-compat facade re-exporting both crates
]
resolver = "2"
